# hubspot-tickets dry-run — post-rework routing verification

- **run_id**: verification-pass-01
- **run_started_at_utc**: 2026-04-23T21:59:42Z
- **last_run_at_used**: 2026-04-23T18:54:54.096Z
- **run_mode**: dry_run
- **purpose**: verify the new runtime routing rule (form + Mispack/Carrier → auto-send; else draft) against the live ticket queue without making any HubSpot writes or Gmail sends.

This verification pass does not re-run the classifier or drafter against each ticket — it only applies the step 2f routing rule and confirms the `send_customer_reply.py` helper produces a correct preview payload for the tickets that would take the auto-send path. `state.json` and `pending_actions.json` are NOT updated by this pass (no real processing occurred).

## Candidate tickets (8)

| # | Ticket | source_type | topic_of_ticket | Route | Notes |
|---|---|---|---|---|---|
| 1 | 44754798684 | FORM | Operations Inquiry | DRAFT → queue | zara@ofuure.com · signature-confirmation / chargeback |
| 2 | 43844803890 | FORM | General Inquiry | DRAFT → queue | zara@ofuure.com · "ship order 76062 today" |
| 3 | **44767539490** | FORM | **Carrier Issue (Provide Tracking)** | **AUTO-SEND** | jwilson@gopackn.com · order TEST123, tracking 1Z02D293YW21069996 |
| 4 | 43531213637 | FORM | Operations Inquiry | DRAFT → queue | zara@ofuure.com · Ofuure returns list customer names |
| 5 | 44704941957 | EMAIL | — | DRAFT → queue | Buttercloth marketing (spam-like; expected classifier category: `other_unclassified`) |
| 6 | 44707208507 | EMAIL | — | DRAFT → queue | Slotted newsletter (spam-like; expected classifier category: `other_unclassified`) |
| 7 | 44801741476 | EMAIL | — | DRAFT → queue | Jacob's reply to an earlier test email (internal test cruft; new HubSpot ticket from reply routing — expected path 2 artifact) |
| 8 | **44708236804** | FORM | **Carrier Issue (Provide Tracking)** | **AUTO-SEND** | jwilson@gopackn.com · order 28931, tracking 1Z02D293YW31486365 |

## Routing rule applied

```
is_auto_send =
  source_type == "FORM"
  AND topic_of_ticket in ["Carrier Issue (Provide Tracking)", "Mispack (Provide Order Number)"]
```

- 2 tickets match (AUTO-SEND): #44767539490, #44708236804 — both FORM + Carrier Issue
- 6 tickets fall to DRAFT: everything else
  - 3 FORM tickets with non-auto-send topics (Operations Inquiry, General Inquiry)
  - 3 EMAIL tickets (non-FORM can never auto-send by rule)

## Auto-send helper dry-run payloads (verified)

### Ticket #44767539490

```json
{
  "mode": "dry-run",
  "gmail_preview": {
    "from": "Pack'N Customer Care <lconner@gopackn.com>",
    "to": "Jacob Wilson <jwilson@gopackn.com>",
    "reply_to": "customercare@gopackn.com",
    "subject": "Re: Carrier Issue — Order #TEST123"
  },
  "hubspot_engagement_preview": {
    "endpoint": "POST /engagements/v1/engagements",
    "ticket_id": "44767539490",
    "contact_id_lookup": 191209416790
  }
}
```

### Ticket #44708236804

```json
{
  "mode": "dry-run",
  "gmail_preview": {
    "from": "Pack'N Customer Care <lconner@gopackn.com>",
    "to": "Jacob Wilson <jwilson@gopackn.com>",
    "reply_to": "customercare@gopackn.com",
    "subject": "Re: Carrier Issue — Order #28931"
  },
  "hubspot_engagement_preview": {
    "endpoint": "POST /engagements/v1/engagements",
    "ticket_id": "44708236804",
    "contact_id_lookup": 191209416790
  }
}
```

Contact resolution works for both. Subject composition from generic `subject` + topic + order_number works. HubSpot POST target is `/engagements/v1/engagements` (the timeline-visible path, per CLAUDE.md invariant #11).

## Counters (simulated, dry-run)

```
tickets_matched            = 8
tickets_processed          = 8
tickets_skipped_dedupe     = 0   (skipped — verification pass didn't hydrate num_notes)
tickets_skipped_error      = 0
notes_posted               = 0   (would be 6 on live run)
auto_sends                 = 0   (would be 2 on live run)
auto_send_failures         = 0
urgent_emails_drafted      = 0   (would depend on classifier output)
tickets_queued_for_digest  = 0   (would be 6 on live run)
image_errors               = 0
mcp_errors                 = 0
```

## Outcomes verified

- ✅ Routing rule correctly splits FORM Mispack/Carrier from everything else (2 auto-send / 6 draft).
- ✅ Helper `send_customer_reply.py` dry-run produces the expected payload shape for both auto-send candidates.
- ✅ Subject composition substitutes topic + order_number when ticket subject is generic "New ticket created from form submission".
- ✅ Contact lookup from the ticket succeeds (contactId 191209416790 resolved for both auto-send tickets — same internal test contact).
- ✅ Dry-run mode causes zero Gmail sends and zero HubSpot engagements.
- ⚠ Expected: on live, ticket #44708236804 will likely be dedupe-skipped (it has a fingerprint with num_notes=1 but the ticket now has multiple engagements from today's testing). Not a bug — this is existing dedupe behavior on an already-processed ticket.

## Next step

Flip `config/settings.yaml:dry_run` back to `false` and invoke the skill again for a live pass. On live:
- Ticket #44767539490 should auto-send to jwilson@gopackn.com, creating Gmail message + v1 email engagement + v1 note engagement, and NOT queuing to pending_actions.json.
- The 6 draft-path tickets should each get an internal `[DRAFT — REVIEW BEFORE SENDING]` note and a record in `pending_actions.json` with the new per-ticket schema (drafted_reply_body, nested action_items with needs_hubspot_reply flags, urgent_emailed marker).
- The digest run (`/packn-digest`) should render the new per-ticket format.
