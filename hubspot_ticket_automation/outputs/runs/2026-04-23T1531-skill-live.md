# Run log — 2026-04-23T15:31Z (run #3, skill-live)

## Mode
- `dry_run: false` (live)
- `per_run_cap: 25`
- Invoked via `/packn-tickets` (skill: hubspot-tickets)
- First run after the `hs_file_upload` hardening fix (attachment URLs no longer hydrated)

## Filter window
- `last_run_at` at start: `2026-04-23T14:09:00Z`
- Stages: `[1, 3]`
- `hs_last_message_from_visitor = true`
- Pipeline: `0`

## Candidate tickets
- **Total matched: 1** — ticket #44708236804 (Jacob Wilson, "New ticket created from form submission", Carrier Issue, tracking 1Z02D293YW31486365)

## Processed: 0
## Skipped: 1

### Skip detail — ticket #44708236804
- **Reason**: dedupe — already in `processed_ticket_ids` from run #2; re-matched because our own note-post in run #2 bumped `hs_lastmodifieddate` to `2026-04-23T14:09:08.311Z` (8 seconds after the saved `last_run_at` of 14:09:00Z).
- **Verification**: `num_notes: "1"` on the ticket matches `notes_posted_by_run.2 = [108455008390]` → our draft is present. Associated-emails search returned 0 results → no new customer activity since last processing.
- **Action**: none posted. Draft from run #2 remains in place for reviewer action.

## Notes posted: 0
## Urgent emails drafted: 0
## Normal items queued: 0

## State changes
- `last_run_at` advanced: `2026-04-23T14:09:00Z` → `2026-04-23T14:09:08.311Z` (to prevent the 8-second drift from re-matching this ticket indefinitely).
- `processed_ticket_ids`: unchanged (44708236804 already listed).
- `run_count`: 2 → 3.

## MCP errors
- None. **No `Could not process image` errors** — the attachment-hardening fix held on this run (though this ticket did not appear to carry file uploads anyway; true validation requires processing a ticket with `hs_file_upload` set).

## Open issues / observations
1. **Re-match risk persists on already-processed tickets**: tickets where `hs_last_message_from_visitor` stays `true` (because we only posted an internal note, not a public reply) will continue to match the filter on every future run. The skill deduplicates by `processed_ticket_ids`, but the check is implicit. Consider: add an explicit "skip if ticket_id in processed_ticket_ids AND no associated email created after our most recent note" rule to the main loop in `SKILL.md`.
2. **Form-sourced tickets have no associated `emails` objects via the current association query**: searching `emails` with `associatedWith tickets/EQUAL/<id>` returned 0 for #44708236804, but `num_notes: 1` and the ticket has a form body. The form submission may be stored as a different engagement type (e.g., `notes` or a custom object), or the association filter syntax may be off. Flag for investigation if a genuinely new form ticket arrives and fails to hydrate. (Did not affect run #2, which successfully drafted a note — suggests prior hydration used a different path.)
